import { useState } from 'react';
import { useRouter } from 'expo-router';
import { Pressable,ScrollView,Text,TextInput,View } from 'react-native';
import { signInOwner } from '@/services/controlPlane';

export default function OwnerSignIn(){const router=useRouter();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[busy,setBusy]=useState(false);const[error,setError]=useState<string|null>(null);async function signIn(){setBusy(true);setError(null);try{await signInOwner(email,password);router.replace('/')}catch(c){setError(c instanceof Error?c.message:String(c))}finally{setBusy(false)}}return <ScrollView contentContainerStyle={{flexGrow:1,justifyContent:'center',padding:24}}><View style={{gap:14}}><Text style={{fontSize:28,fontWeight:'800'}}>Kleenest Owner sign in</Text><Text style={{color:'#607067',lineHeight:21}}>This private app verifies platform-owner or administrator authority after authentication. Unauthorized sessions are immediately signed out.</Text>{error?<Text style={{color:'#9b2c2c'}}>{error}</Text>:null}<TextInput value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address" placeholder="Email" style={input}/><TextInput value={password} onChangeText={setPassword} secureTextEntry placeholder="Password" style={input}/><Pressable disabled={busy||!email.trim()||!password} onPress={signIn} style={{backgroundColor:'#132b21',padding:14,borderRadius:14,opacity:(busy||!email.trim()||!password)?0.5:1}}><Text style={{color:'white',fontWeight:'800',textAlign:'center'}}>{busy?'Verifying owner access…':'Sign in'}</Text></Pressable></View></ScrollView>}
const input={borderWidth:1,borderColor:'#ccd9d1',borderRadius:14,paddingHorizontal:14,paddingVertical:13,backgroundColor:'white'} as const;
