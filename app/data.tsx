import { useState } from 'react';
import { ScrollView,Text,View } from 'react-native';
import { Action,ErrorBanner,Field,Hero,JsonPanel } from '@/components/OwnerUI';
import { getSession,ownerCrudList } from '@/services/controlPlane';
const resources=['profiles','businesses','fleets','locations','support_requests','external_data_sources','external_data_datasets','national_ingestion_markets','national_ingestion_runs'] as const;
export default function DataWorkbench(){const[resource,setResource]=useState<(typeof resources)[number]>('businesses'),[data,setData]=useState<unknown>(null),[busy,setBusy]=useState(false),[error,setError]=useState<string|null>(null);async function load(){setBusy(true);setError(null);try{if(!await getSession())throw new Error('Platform Owner authentication required.');setData(await ownerCrudList(resource))}catch(e){setError(e instanceof Error?e.message:String(e))}finally{setBusy(false)}}return <ScrollView contentInsetAdjustmentBehavior="automatic" contentContainerStyle={{padding:16,gap:14,paddingBottom:50}}><Hero eyebrow="AUDITED ADMIN GATEWAY" title="Data workbench" body="Inspect approved canonical resources through owner-authorized operations with no direct security bypass."/><ErrorBanner message={error}/><Field value={resource} onChangeText={value=>setResource(value as typeof resource)} autoCapitalize="none" placeholder="Canonical resource"/><View style={{flexDirection:'row',flexWrap:'wrap',gap:8}}>{resources.map(value=><Action key={value} label={value} onPress={()=>{setResource(value);setData(null)}}/>)}</View><Action label={busy?'Loading…':'List resource'} disabled={busy} onPress={load}/>{data?<JsonPanel title={resource} value={data}/>:<Text selectable>Select an approved resource and load its current server state.</Text>}</ScrollView>}
