name: Retired — standalone Owner OTA

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Production authority moved
        run: |
          echo "Kleenest_Owner no longer publishes to owner-production."
          echo "Owner OTA authority is Kleenest_Production/apps/platform-mobile on main."
